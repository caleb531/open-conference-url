name: Release

on:
  push:
    tags:
      - 'v*'

jobs:
  publish:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2

      - name: Build Alfred workflow
        id: builder
        uses: mperezi/build-alfred-workflow@master
        with:
          workflow_dir: .
          exclude_patterns: .git/* .github/* .gitattributes .gitignore .python-version .editorconfig packager.json requirements.txt screenshot*.png

      - name: Create release
        id: create_release
        uses: actions/create-release@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          tag_name: ${{ github.ref }}
          release_name: ${{ github.ref }}
          draft: false
          prerelease: false

      - name: Upload release asset
        uses: actions/upload-release-asset@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          upload_url: ${{ steps.create_release.outputs.upload_url }}
          asset_path: ${{ steps.builder.outputs.workflow_file }}
          asset_name: ${{ steps.builder.outputs.workflow_file }}
          asset_content_type: application/zip
